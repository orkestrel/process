1. **CONFIRMED** — `pid`, `code`, and `signal` are required readonly members in `src/core/types.ts:157-172`. Their getters read `#child` directly without duplicate state at `src/server/Process.ts:58-85` and `src/server/Process.ts:149-162`. The interface remarks cover eager observation, permanent failed-spawn absence, survival after exit, lack of liveness meaning, and native-exit versus stdio-close timing at `src/core/types.ts:157-164`. Guide parity and derived liveness appear at `guides/process.md:177`, `guides/process.md:202`, `guides/process.md:404-417`, `guides/process.md:708`, and `guides/process.md:1002-1004`.

2. **BROKEN** — The failed-spawn regression does not fail on the pre-change `pid` defect named by its title. With the parent implementation, `child.pid` is an absent property and therefore reads as `undefined`, while `child.exit` already settles. Those named assertions pass. The failure occurs because the unnamed `code` and `signal` reads produce `undefined` instead of the expected `null` at `tests/src/server/Process.test.ts:407-426`; the getters were absent before the additions shown at `tmp/followup-a-diff.patch:704-741`. Add a runtime presence assertion for `pid` and name the terminal-pair assertions in the test title. The adjacent evidence holds: the descendant inherits stdout at `tests/src/server/fixtures/child.mjs:42-59`, and the test observes a pending `exit` together with `code === 0` at `tests/src/server/Process.test.ts:457-478`. The spawn-fault proof asserts a negative code and equality with `exit.code`, not a platform errno value, at `tests/src/server/Process.test.ts:421-426`. The tests drive real child processes and real scratch directories.

3. **CONFIRMED** — `executeSync` reads every option into a local before validation at `src/server/handlers.ts:221-245`; `detach` does the same at `src/server/handlers.ts:294-301`. The pre-change repeated reads are preserved in the diff evidence at `tmp/followup-a-diff.patch:756-800`, and the mutating-getter proofs exercise the mismatch at `tests/src/server/handlers.test.ts:38-59` and `tests/src/server/handlers.test.ts:63-83`. `execute` retains the same discipline at `src/server/handlers.ts:57-88`.

4. **CONFIRMED** — The Windows simultaneity statement matches the single `taskkill /F /T` invocation at `src/server/helpers.ts:648-658` and is restored at `src/server/helpers.ts:691-698` and `guides/process.md:383-395`. The README names the signal-taking and abstaining entry points at `README.md:14-16`. The read-once invariant appears in the type contract at `src/core/types.ts:282-313` and the guide at `guides/process.md:568-573` and `guides/process.md:710-713`. The restored `pid` offer appears at `guides/process.md:706-708`.

5. **BROKEN** — The report presents every Process regression as binding to its named defect at `tmp/followup-a-report.md:24-46`, but it omits that the failed-spawn test’s `pid` and `exit` expectations already pass when the `pid` getter is absent. That omission makes the statement that no criterion remained open at `tmp/followup-a-report.md:3-4` false. Correct the report after adding the missing runtime-presence pin and renaming the test to cover its terminal-pair assertions. The Linux-only caveat and the refusal to pin the errno value are accurate at `tmp/followup-a-report.md:130-138`.

VERDICT: FAIL — 2 broken, 0 unresolved, 0 not-evidenced, 0 findings outside the claims