4b01d30 Gate the published artifact, and correct the prose that ships with it
PC4 closes Q9-Q14. The distribution proof is the load-bearing change: it packs the
package, installs the tarball into an external temporary consumer, resolves every
export target, and compares the runtime export sets against the compiler-parsed
declaration exports. A consumer now finds out at publish time when a subpath stops
resolving, rather than after installing.

The brief carried a stale server count of 29. The compiler disagreed: core exposes 13
runtime and 13 declared values, server 33 and 33, measured independently and in
agreement. The gate asserts the measured numbers.

The prose repairs are all cases where the file said something the code does not do:

- `quoteArgument('%1')` returns `"%1"`. The remark claimed the token was left
  unquoted. The runtime is correct and unchanged; `buildSpawn` refuses `%` in an
  argument to a Windows batch target, which is the real protection.
- `isolated: true` leaves no `PATH` on POSIX, so an absolute `file` or an explicit
  `PATH` is required. On Windows libuv injects a host set regardless.
- A spawn fault reports the host's negative errno for `run` and `null` for `runSync`.
- A detached POSIX child survives the supervisor's SIGKILL and never receives the
  terminal's SIGINT, so an orderly shutdown must call `stop` or `destroy`.
- `README.md` states the supported `moduleResolution` values.

Red/green on the guide proof: before the prose changes exit 1 with 3 failed, after
them exit 0 with 4 passed. The distribution proof's negative control inverted the
absent-subpath assertion and failed exit 1 on ERR_PACKAGE_PATH_NOT_EXPORTED, then
passed exit 0 restored.

Gates, run on the host on 2026-08-20 because the bench sandbox denies child process
creation: format:check, lint:check, check, build, test:distribution --mode release,
test:config, and test all exit 0.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KgYWuAE9LECgKRJE3ARgJV

---
52edc64 Brief PC4: a gate over the published artifact, and the prose that ships
Six consumer-facing rows. Q13 is the structural one: every defect this package
carried passed all five gates, because nothing loads the artifact a consumer
installs. Closing the same hole in the sibling package immediately exposed a
defect six green gates had shipped past.

Carries the sibling's mistake as a warning: its distribution test linked peers
only inside the extraction fallback, so on a host where the real install
succeeds the consumer had no toolchain and the gate failed for a reason its
sandbox could never show.

Refuses typesVersions for Q14 on the audit's ruling - the mode is deprecated in
TypeScript 6 and removed in 7, so the workaround would outlive its subject.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KgYWuAE9LECgKRJE3ARgJV

---
ed70ad2 Prove the decidable half of the Windows surface on a POSIX host
Nine tests skipped on Linux, the cmd.exe and taskkill branches shipped in the
tarball, the guide documented Windows as covered, and the last commit changed
the fixtures those tests depend on reasoning from a platform difference it never
re-ran. The audit ruled that a supported platform with zero executed evidence
after such a change is an unproven seam rather than a known limit.

Environment lookup and merging, PATHEXT candidate ordering, batch routing,
quoting, and the percent-sign refusal are decisions rather than I/O, so they now
take the platform as a parameter and both branches are exercised from this host.
process.platform reads in src/server/helpers.ts fall from 7 to 3, and each
survivor drives signalling, termination, or spawn options.

The first extraction attempt was refused for the right reason: wrappers that
re-read the host preserve every original read, which relabels the problem rather
than reducing it. The boundary moved so the entry points pass the platform in.

killTree through taskkill.exe and grandchild tree termination stay unproven,
named exactly, with the settling command recorded in the guide and the handoff.
The extraction proves the decisions, not Windows end to end, and both documents
say so in those words.

The mutation control fired on exactly one named candidate-order test and the
restored code passed the same command, so the extraction is load-bearing rather
than decorative.

Host gates, each by direct exit: format:check, lint:check, check, build 0;
test:src 106 passed 7 skipped, policy 86, setup 6, config 28, guides 55; npm
install of the packed tarball 0. Skips fall from 9 to 7.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KgYWuAE9LECgKRJE3ARgJV

---
2433bed Amend PC3: Q7 is closed, and waitForCondition now exists
The unit was dispatched with Q7 open and killed before it started work. Q7 was
then closed by the Orchestrator at 16ec17f, so a stale brief would send the unit
to redo settled work and probably re-break it.

Also points it at tests/setup.ts's waitForCondition rather than letting it write
another local polling loop.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KgYWuAE9LECgKRJE3ARgJV

---
e1f36c7 Take the scaffold overwrite that registers the setup project
Adding tests/setup.test.ts makes scaffold derive a setup Vitest project from
the proof file's presence. overwrite wrote it into vite.config.ts and removed
nothing.

Gates after the overwrite, each by direct exit: format:check, lint:check,
check, and build 0; test:src 102 passed 9 skipped, policy 86, config 28,
guides 55, setup 6.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KgYWuAE9LECgKRJE3ARgJV

---
fb99c37 Declare the test:setup script the setup project requires
Adding tests/setup.test.ts makes scaffold register a setup Vitest project, and
its plan requires the manifest to reach that project from the test chain.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KgYWuAE9LECgKRJE3ARgJV

---
16ec17f Restore the two proofs a bad revert destroyed, and close Q7's race at its cause
My git checkout of tests/src/server/helpers.test.ts reverted the whole file to
the last commit rather than the five lines I had written, discarding unit PC2's
entire contribution to it. Commit 64c5b83 therefore carried Q7's and Q8's source
repairs with neither proof, and its message claimed both were implemented. The
source half was true, the proof half was not.

Q8's proof is rewritten against the real echo fixture: a NUL inside the stdin
payload survives the transfer, which proves input is sent as bytes rather than
rejected. Passing runSync a raw string previously failed with Unknown encoding:
buffer.

Q7's proof is rewritten and its race closed at the cause rather than papered
over. Three earlier attempts were wrong and each looked right: polling for the
marker converted a fast failure into a five-second timeout; a poll budget equal
to the test's own timeout can never fail gracefully; and a fixture readiness
line still races if the root dies before the grandchild can write it. The actual
defect was the test's own parameter. The root ran with timeout 50 while Node
bootstraps in 45.7 to 49.9 ms on this host, so the grandchild was racing its own
interpreter startup and lost three times in six. The root now runs 400 ms, well
clear of startup, so the test measures termination rather than bootstrap.

Adds waitForCondition to tests/setup.ts, the shared test infrastructure module,
with its own proof. It measures the deadline with performance.now rather than
Date.now: tests.md requires it, and the clause that decides it here is that
Date.now moves when the wall clock does, so a helper whose only job is bounding
a wait would have its deadline shifted by a clock adjustment taken mid-wait. The
condition may be synchronous or asynchronous, because the two races that
motivated the helper both poll the filesystem.

Eight consecutive runs of the file are green.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KgYWuAE9LECgKRJE3ARgJV

---
44a3e1f Brief PC3: shrink the Windows surface, and close the Q7 race
Extracts the decidable platform decisions - PATHEXT folding, batch routing,
quoting, percent refusal - into pure functions taking the platform as a
parameter, so both branches are provable on this host. killTree and grandchild
tree termination stay unproven and are named exactly, with the command that
settles them.

Carries the rule the existing skips already break: tests.md requires a
conditional skip to cite the mechanism, not the platform name alone, and all
fourteen cite process.platform only. That is why nine tests sat unexecuted with
nobody re-examining them.

Adds Q7 with its measurement: the documented runSync-leaves-descendants claim is
a race the grandchild loses about half the time, because it is racing Node's own
45.7 to 49.9 ms bootstrap against a 50 ms deadline. The fixture must signal
readiness and the guide sentence must be qualified. Records that a polling-only
repair was attempted and reverted.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KgYWuAE9LECgKRJE3ARgJV

---
64c5b83 Cap the teardown drain, and correct the bound that hid it
stop() retained unbounded memory: measured at backlog 1024 against a
SIGTERM-trapping flooding child, one call drained 29,721,480 bytes and grew the
parent heap 45 MB. Teardown now retains at most twice the backlog - one ordinary
budget and one drain budget - trims existing excess before stdout resumes, and
drops later lines.

It never re-pauses the reader, which is the constraint that makes this a fix
rather than a trade. A paused stdout holds the child's own write and therefore
its exit, so re-applying backpressure during termination would replace a memory
bound with a hang. Omission surfaces through Process.truncated, reusing the term
the package already has for output the consumer did not receive.

HANDOFF.md stated the overshoot bound as one delivered chunk plus one line,
which the measurement falsifies by four orders of magnitude. That written bound
was itself a defect - the kind of stale claim a later campaign reads as current -
and it now states what the code does.

Q8 ruled: input is stdin payload and carries no NUL restriction. runSync now
converts string input to bytes, fixing its prior Unknown encoding: buffer
failure.

Q7 is NOT closed and its row is reopened in the grade with measured evidence.
The documented claim that a runSync timeout leaves descendants running is a
race: six trials show the grandchild writing at ~295ms in three and never in the
other three, because it is racing its own Node bootstrap - measured by this
campaign at 45.7 to 49.9 ms - against the root's 50 ms deadline. The fixture
must signal readiness the way the trap fixture does before the claim can be
tested or stated, and that is a design decision routed to the next unit rather
than a flake repair. A polling patch was attempted and reverted: it converts a
fast failure into a five-second timeout without touching the race.

Also recorded: @orkestrel/test publishes waitForDelay and no wait-until-condition
helper, and two packages have now independently written fixed-sleep races for
want of it.

Host verification, each gate by direct exit: format:check, lint:check, check,
build 0; test:src 102 passed 9 skipped, policy 86, config 28, guides 55; npm
install of the packed tarball 0.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KgYWuAE9LECgKRJE3ARgJV

---
a71089a Brand the error, and spawn the command that was validated
Two release blockers from the production-readiness grade.

isProcessError returned false for a genuine ProcessError thrown by a different
installed copy, and across this tarball's own ESM/CJS boundary. Two copies
already coexist in this repository's tree, and the 0.0.4 cascade widens that
window across mcp, scaffold, probe, and supervisor - so the guard would have
silently disowned failures this package itself threw, in exactly the release
that creates the condition.

The fix is a Symbol.for brand checked alongside the native Error base, the
subclass prototype, the fixed name, and the declared code. A symbol alone is
forgeable, and the control proves the composite is not: a plain Error is
refused, and a plain Error carrying the same brand is also refused. Cross-copy
and cross-format recognition are proven by loading two module instances and by
having the built ESM guard recognize a built CommonJS error.

Every caller-supplied command string was validated on one read and spawned from
another, so a getter answering differently on the second read defeated
validation in all four entry points and raised a host TypeError instead of the
ProcessError coded invalid that every @throws tag promises. Process, run,
runSync, and detach now copy file, arguments, environment, input, and isolation
once into a frozen snapshot, and validation, formatting, environment merging,
and spawning all read that snapshot.

Two measured handle leaks close with it: destroy left the caller's abort
listener attached, and waitForExit retained its exit listener when the deadline
won - twelve retained listeners before, zero after.

Verified on the host, where the executor's sandbox denied nested child creation:
the runSync half of the snapshot proof passes, and the packed tarball installs
under npm 10.9.7 with exit 0.

Gates by direct exit: format:check, lint:check, check, build all 0; test:src 98
passed 9 skipped, policy 86, config 28, guides 51.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KgYWuAE9LECgKRJE3ARgJV

---
3be79b0 Record the production-readiness grade: four blockers before the 0.0.4 window
Six blind lanes, three per package, graded on coverage. This package is in a
different class from probe: its published surface is executed end to end and
holds, both entries load and run in both formats under npm and pnpm, export
parity is exact both directions, and every documented example runs.

Four defects still block. stop() drained 29,721,480 bytes and grew the heap
45 MB against a documented overshoot bound of one chunk plus one line, which is
falsified by four orders of magnitude. Every caller-supplied command string is
validated on one read and spawned from another, so a getter answering
differently on the second read defeats validation in all four entry points and
the promised ProcessError coded invalid never appears. isProcessError returns
false for a genuine ProcessError from another installed copy and across this
tarball's own ESM/CJS boundary. Windows has no executed evidence, and commit
b392629 changed the flooding fixtures on the strength of a platform difference
without re-running the platform it changed them for.

The cross-copy guard is time-critical: 0.0.4 is what opens the multi-version
window across mcp, scaffold, probe, and supervisor that the defect needs.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KgYWuAE9LECgKRJE3ARgJV

---
9e5fa4b Take the scaffold 0.0.44 overwrite and bump to 0.0.4
Audit reported one drift: the guides project lacked an exclude, so it could
collect src and app tests. scaffold overwrite replaced vite.config.ts with the
0.0.44 plan's version, adding that one line, and regenerated the catalog table.
It removed nothing. All six Vitest projects survive.

Version bumped from what the registry serves, 0.0.3, rather than from a local
manifest that can sit a release behind.

Gates after the overwrite and the bump, each by direct exit: format:check,
lint:check, check, and build exit 0; test:src 90 passed 9 skipped, test:policy
86, test:config 28, test:guides 51.

This release does not publish itself. Two packages depend on this one at
runtime, so publishing obliges mcp 0.0.20 and scaffold 0.0.45 in the same
window; .orkestrel/process/0.0.4-release-sequencing.md carries the order.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KgYWuAE9LECgKRJE3ARgJV

---
4e72840 Re-pin @orkestrel/scaffold to ^0.0.44
Registry serves 0.0.44 and this tree pinned ^0.0.43.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01KgYWuAE9LECgKRJE3ARgJV

---
