1. **CONFIRMED** — A full search of every `limit`, `truncated`, `overflow`, and captured-length statement in `guides/process.md` found no remaining length-based identification advice. The attack failed at lines 602–606: the guide explicitly says neither captured length identifies the overflowing stream.

2. **BROKEN** — The count sweep found correct tallies for three tiers, six `ProcessChild` members, five error codes, four result flags, three `Process` methods, three `stop` overloads, two termination hosts, two runners, three launch refusals, and one error class. However, `guides/process.md:960` introduces “Three names,” while lines 965–969 contain five naming rulings. Change the introduction to five rulings or remove the count.

3. **BROKEN** — `.orkestrel/process/readiness-grade.md:44` still says twelve exports appear in no guide fence and then says all sixteen claims are true. `tests/guides.test.ts:851` and the compiler-derived inventory establish sixteen: fourteen `EXAMPLES` rows plus `retainChunk` and `resolveExecutable`. Correct the retained record’s twelve to sixteen.

4. **CONFIRMED** — The whole tracked-tree search found no live `runSync` or `Run*` identifier. Every `run` survivor is an ordinary invocation noun or verb, an `npm run`/`vitest run` command, a framework method such as `tester.run`, an unrelated fixture or package API, or a historical campaign record describing the pre-rename surface. The two `Runs` occurrences in `src/server/helpers.ts` are English TSDoc verbs.

5. **CONFIRMED** — Reviewing every PC7 replacement found no ordinary use renamed. Each changed occurrence was the former one-shot function, its associated `Run*` type, factory or helper, a code token naming that surface, or prose and test names specifically describing that primitive. No unrelated caller pattern changed.

6. **CONFIRMED** — The three behavioral tests retain their controls: plain-`Error` refusal, the outside-tuple `stalled` refusal, and distinct-copy recognition with plain and branded-lookalike refusals. `npm run test:src:core` executed the sole file and reported 3 passed. Barrel membership remains covered by guide/source parity and the built-artifact runtime/declaration comparison.

7. **CONFIRMED** — A negative control removed the real server barrel’s helpers row while parsing the real guide. The live comparison returned no missing symbols; the controlled comparison reported `function execute` and the other omitted helpers. The assertion derives its expected side from `guides/process.md`, not a duplicated export literal.

8. **BROKEN** — Three likely-vacuous instruments were attacked. The missing-server-export control failed as required, and an added unlisted test was detected. The stream-origin test did not bind its claim: an `ExecuteResult` augmented with `stdoutTruncated: false` and `stderrTruncated: true` still satisfied all assertions at `tests/guides.test.ts:474–490`. This directly contradicts the comment at line 468 that a later per-stream field would break the test. Assert the exact result property set and include the augmented result as a negative control. These attacks did not reach the repository’s `tests/setup*.ts` helpers.

9. **UNRESOLVED** — The stream ambiguity, command snapshot, output bounds, environment merge, manager refusals, error identity, and POSIX termination statements matched source and executed evidence. The live Windows claims cannot be decided here: `guides/process.md:978–981` identifies current `killTree` through `taskkill.exe` and grandchild termination as unproven. Running the command at lines 983–984 on a Windows host against the current fixtures would settle them.

10. **CONFIRMED** — Independent enumeration produced 67 declarations on both the barrels and guide: the core face has 8 constants, 6 error values, and 18 types; the server face has `ProcessChild`, 2 classes, 2 factories, and 30 helpers. Methods match independently: `ProcessInterface` has `send`, `stop`, and `destroy`; `ProcessManagerInterface` has `process`, `processes`, `launch`, the three `stop` overloads, and `destroy`. The manifest exposes exactly `.`, `./server`, and `./package.json`. Removing the server helpers row fired the missing-direction control.

11. **BROKEN** — I would not publish this tip. No legitimate caller pattern in the tree broke, and no evidence contradicts the measured scaffold or MCP usage, but the package still contains the false five-as-three guide count, the retained twelve-as-sixteen record, an unfired Windows residue, and a parity test whose claimed future-field control does not bind. Correct claims 2, 3, and 8, then obtain the Windows evidence required by claim 9 before publication.

VERDICT: FAIL — 4 broken, 1 unresolved, 0 not-evidenced, 0 findings outside the claims